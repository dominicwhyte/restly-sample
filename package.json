{
  "name": "restly-sample",
  "version": "1.0.0",
  "description": "An example Restly repo",
  "main": "build/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc --build",
    "deploy": "tsc --build && git add . && git commit -m \"update\" && git push origin main"
  },
  "author": "",
  "license": "ISC"
}
